schemas:
  User:
    type: object
    required:
      - email
      - password
      - nickname
    properties:
      id:
        type: number
        description: 고유값
      email:
        type: string
        format: email
        description: 이메일
      password:
        type: string
        description: 비밀번호
      nickname:
        type: string
        description: 별명
      status:
        type: string
        description: 일반 유저, 관리자, 정지 유저
      introduce:
        type: string
        description: 한 줄 소개
      profileImage:
        type: string
        description: 프로필 사진 url
  GeneralError: 
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
responses:
  NotFound:
    description: URL not found.
  IllegalInput:
    description: Illegal input for operation.
  GeneralError:
    description: General Error
    content:
      application/json:
        schema:
          $ref: '#/schemas/GeneralError'